# Admin panel password
ADMIN_PASSWORD=

# Secret used to sign JWT tokens — generate a long random string for production
JWT_SECRET=

# Path to the SQLite database file (relative to project root)
DATABASE_PATH=./data/gallery.db
